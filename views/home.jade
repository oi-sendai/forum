extends layout

block content
  .row.hemp
      .col-md-12
        h1 Welcome to Scotland 2.2
  p.lead Happy democracy everyone.

  hr
  section
    .row
      for category in categories
        .col-sm-6
          h2 #{category.name}
          p #{category.description}
          a.btn.btn-default(href='/room/#{category._id}', role='button') View details »

